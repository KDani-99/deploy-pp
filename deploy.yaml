apps:
  - name: "First Application"
    repository: "<link>"
    local: "<local repo path>"
    secret: "" # GitHub webhook secret for this repository (used for request validation)
    steps: # After pulling the repository
      - name: "Install dependencies/updates"
        cmd: "npm i --save"
      - name: "Restart app"
        cmd: "pm2 restart <app name>"